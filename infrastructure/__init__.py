"""Infrastructure as Code modules using AWS CDK.

These files are optional at runtime and only required during deployment
operations.  Import errors are handled gracefully so the rest of the codebase
can be imported without AWS CDK installed.
""" 